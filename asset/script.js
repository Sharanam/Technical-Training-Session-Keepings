const allElements = "#intro,#projects,#experience,#confabulate";
// function changeView(id) {
//   id = id.match(/#(.*)/);
//   id = id?.length > 1 ? id[1] : "intro";
//   if (allElements.includes(id)) {
//     $(allElements).hide();
//     $(`#${id}`).show();
//   }
//   setTimeout(() => window.scrollTo(0, 0));
// }
$(document).ready(function () {
  // changeView(location.href);
});
